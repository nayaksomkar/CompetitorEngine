# Taste File
- Prefers Python with FastAPI for building microservices. Confidence: 0.95
- Uses Pydantic v2 for data validation at every boundary. Confidence: 0.95
- Favors async/await patterns throughout (async HTTP clients, async endpoints). Confidence: 0.9
- Organizes code into clear modular layers: schemas, agents/services, routers, orchestrator. Confidence: 0.9
- Uses logical agent classes (not separate services) for workflow steps within a single microservice. Confidence: 0.85
- Prefers lightweight services: no database, Redis, or authentication unless explicitly needed. Confidence: 0.9
- Designs swappable provider interfaces (e.g., abstract base class + mock + HTTP implementations) for external service integration. Confidence: 0.9
- Communicates with LLM services via HTTP using simple JSON payloads like `{"query": "..."}`. Confidence: 0.85
- Uses versioned REST API endpoints (e.g., `/api/v1/analyze`). Confidence: 0.85
- Returns structured frontend-ready JSON outputs with explanation fields for "Explain This" functionality. Confidence: 0.9
- Includes source/evidence references and confidence scores in response metadata. Confidence: 0.85
- Expects retries with exponential backoff and configurable timeouts for external HTTP calls. Confidence: 0.9
- Uses pydantic-settings for environment variable configuration with `.env.example` files. Confidence: 0.9
- Prefers structlog for structured logging. Confidence: 0.8
- Includes health check endpoints (`/health`) in services. Confidence: 0.85
- Writes pytest tests with mocking (AsyncMock) for unit and integration tests. Confidence: 0.85
- Provides sample JSON data files for API documentation, test fixtures, and GitHub reference. Confidence: 0.8
- Prefers simple, minimal README documentation written in layman language — avoids verbose JSON data examples and excessive detail. Keeps only essential info (what the service does, how to run, key endpoints, field names). Confidence: 0.85
- Uses Docker with healthcheck instructions for containerization. Confidence: 0.85
- Provides highly detailed, structured requirements upfront covering architecture, components, data flow, and tech stack. Confidence: 0.9
- Expects a plan/review step before large implementations. Confidence: 0.8
- Wants CORS middleware mounted FIRST (outermost) so it wraps every route; echoes specific requesting origins in `Access-Control-Allow-Origin` (no wildcard `*`); requires OPTIONS preflight to return 200/204 with CORS headers — not 400; explicitly does not modify existing routes, response shapes, or unrelated functionality when adding CORS. Confidence: 0.9
- Prefers flat repository structure: when a repo IS a single microservice, code lives at the root, not nested inside a subfolder like `microservice1/`. Confidence: 0.8
- Each backend microservice README must include a clear "API Contract" section with exact request/response JSON schemas so the UI team knows exactly what to send and render. Confidence: 0.95
- Backend services should document UI integration instructions (how to render charts, tables, cards, handle "Explain This" tooltips with explanation fields). Confidence: 0.9
- Backend microservice docs must include the full runtime topology — Docker container layout, port mappings, environment-variable wiring between services, and how the UI reaches each service — so consumers know exactly how the system is deployed and how to connect to it from outside the container network. Confidence: 0.9
- Wants taste/preference learnings recorded at the global root level (`taste.md`), not in project-specific subdirectories, so preferences apply across all projects. Confidence: 0.9
- Prefers hardcoded single-file HTML UIs (inline CSS/JS, no build steps, no frameworks) over separate frontend frameworks for simple interfaces. Confidence: 0.7
- Uses CSS-only responsive design with media queries (no external UI libraries) for multi-screen compatibility. Confidence: 0.7
- Communicates in short, terse, typo-prone messages and expects the assistant to investigate proactively (check git history, read config, inspect Dockerfile, etc.) before asking clarifying questions — rather than waiting to be told what to look at. Confidence: 0.7
- Prefers Render as a deployment platform for Python/FastAPI services. Confidence: 0.7
- Prefers minimal, surgical code changes — explicitly does not want existing routes, business logic, response shapes, or unrelated functionality touched when making a targeted fix. Confidence: 0.85
- Does not accept superficial verification (e.g., "config loads correctly") as proof a fix works — pushes for thorough end-to-end testing that actually validates the behavior before claiming success. Confidence: 0.75
- When adding a new agent/code path that consumes LLM responses, must update existing test mocks (e.g., `mock_llm.send_query.side_effect` lists) to provide the extra responses the new code path will consume — failing tests due to mock exhaustion after adding a feature is treated as a bug in the change, not the test. Confidence: 0.85
- Demands Docker-based local verification for backend fixes — wants the actual Docker image built, the container running, and the new behavior exercised against the live process (not just `pytest` or local Python). Saying "tests pass" is not enough; the user wants `docker build` + `docker run` + `docker exec` confirmation. Confidence: 0.85
- Wants a strict workflow sequence for fixes: first push the code to the remote, then test it locally to confirm the actual behavior (e.g., that CORS is enabled and headers appear), and only after that evidence look externally for blame (deployment platform, infra, etc.) — never assume the fix works without a local test confirming the behavior. Confidence: 0.85
- Demands strict separation of concerns across microservices: each service must do exactly one job (e.g., one service = reasoning/LLM, one service = research/scraping, one service = orchestration/routing). An orchestrator service must NOT contain reasoning code, prompting logic, or scraping code — it only delegates via HTTP to upstream services. Confidence: 0.95
- Requires fail-fast startup behavior: services must refuse to start (raise RuntimeError) if required configuration like upstream service URLs is missing, rather than silently running in a broken state. No default service URLs that would mask misconfiguration. Confidence: 0.95
- All service URLs, API keys, and external dependencies must come from environment variables — explicitly forbids hardcoded service URLs, API keys, database connections, or tightly coupled service dependencies in source code. Confidence: 0.95
- More general rule (overrides the earlier "minimal surgical changes" instinct when doing architectural refactors): when explicitly asked for a refactor, prefers aggressive dead-code removal — delete old providers, fallback chains, mock layers, unused config blocks rather than keeping them "for compatibility". Surgical preservation is for targeted fixes; refactors should leave the codebase obviously reflecting the new design. Confidence: 0.9
- Wants visualization data and structured UI responses validated against actual data — never fabricate values. Charts must have non-empty labels and datasets, or be dropped entirely. Metric cards must have numeric values, or be dropped. Empty data points = drop the chart, never invent numbers. Confidence: 0.95
- Prefers stateless services that delegate session/conversation state to an upstream service (e.g., the AI/reasoning service owns session_id and history). The orchestrator should just forward session_id, not maintain local session storage (Redis/in-memory/file). Confidence: 0.9
- For chat/follow-up flows, wants the orchestrator to intelligently decide whether fresh research is needed before calling upstream services (ask the reasoning service "do you need fresh data?" and only call the research service if yes). Avoids wasted upstream calls when existing context suffices. Confidence: 0.9
- Wants validation rules that fail loud (raise exceptions) when upstream services return malformed/incomplete responses, rather than silently producing empty/half-broken UI data. Better to surface the failure than ship a degraded result. Confidence: 0.9
- Provides extremely detailed multi-paragraph architectural specifications upfront in a single message — covering data flow, component boundaries, environment variables, response shapes, validation rules, error handling, and tech stack. Expects the assistant to read the spec carefully and execute it precisely, not ask basic clarifying questions. Confidence: 0.95
- When a deployment platform (Render) has environment variables that can override config defaults, prefers hardcoding security-relevant values like CORS allowed origins directly in code rather than relying on env-var/config chains that can silently break in production. Confidence: 0.85
- When the assistant doesn't know what the user is referring to (e.g., an ambiguous term like "Fragnote"), it should NOT respond with a generic "I'm not sure, could you clarify?" — instead it should either search the web for context or surface relevant keywords from previous chat history as hashtags/keywords/nodes to give itself enough context to answer. Confidence: 0.85
- Before answering, the assistant should pull keywords/hashtags from current and previous chats as contextual anchors so it has enough background to give a concrete, non-absurd answer. Confidence: 0.8
- Wants responses to be quick and decisive — explicitly says "do it quick" and gets frustrated by generic/over-explained responses when a simple keyword lookup would suffice. Confidence: 0.75
- When the AI encounters an unknown term (e.g., a misspelled/unfamiliar brand or product), the preferred handling flow is: (1) collect keywords from the current chat block + previous chat history, (2) combine those keywords with the user's query to build a search query, (3) find the most similar/sensible interpretation via keyword similarity, (4) web-search the top candidates — and answer based on the result. This is "common sense" handling, not a "I don't know, please clarify" response. Confidence: 0.9
- Wants this unknown-term flow implemented in the actual codebase (agents/AI pipeline), not just as a conversational behavior — e.g., a dedicated web-search fallback agent that auto-fires when the LLM doesn't recognize a competitor/term mentioned in the form input. Confidence: 0.85
- Before writing any new code that touches external services (HTTP calls, APIs, scrapers, LLMs), MUST first read the existing codebase to discover and reuse the project's established infrastructure (provider clients, scraper services, LLM clients) — never bypass it by calling external APIs directly with raw httpx/requests when the project already has an abstraction for it. Frustrated when the assistant duplicates or parallels existing infra instead of using it. Confidence: 0.95
- Maintains a split workflow across the stack: the assistant handles backend / Docker / infrastructure / docs in the backend repo, while the user handles the frontend UI themselves. When the user asks for backend changes, they expect structured output (port mappings, env vars, run commands) that they can mirror into their own UI implementation. Confidence: 0.85
- After making documentation or code changes in a backend repo, expects the assistant to commit and push to the remote (with a descriptive commit message and co-authored-by trailer) without being asked — staging, committing, and pushing is the default end-of-task action for backend work. Confidence: 0.85
- For substantial multi-file changes, wants the assistant to write a full action plan to `~/.commandcode/plans/{name}.md`, then `exit_plan_mode` and present a concise summary in chat, then wait for the user to say "yes" before implementing — never start editing files mid-plan. Confidence: 0.9
- When integrating with external services whose contract has drifted or is unknown, first sample the live upstream with a real curl call (`POST /chat`, `POST /research`, etc.) to observe the actual wire shape before designing the adapter — never design the adapter based on assumed/stale documentation. Confidence: 0.9
- Wants wire-level contract tests using in-process FastAPI fake servers (`httpx.ASGITransport`) that capture the actual outbound payload and assert it matches the upstream's expected schema — so future contract drift breaks CI rather than blackholing production requests. Confidence: 0.95
- Prefers an adapter pattern at every client boundary that separates two concerns: (1) building the wire payload from the internal domain vocabulary (`_build_query` / `_build_payload`), and (2) adapting the upstream reply back into the internal keys the rest of the code reads (`_adapt_response`). The orchestrator's internal contract stays untouched — only the thin adapter changes when an upstream contract drifts. Confidence: 0.9
- Prefers graceful degradation on upstream partial responses over hard-fail: when an upstream returns only free-text (`{"answer": "..."}`) instead of the expected structured keys, build a `status="partial"` result from the text fallback rather than raising — but still raise on transport-level failures (timeouts, connection errors) so the caller can distinguish "got partial data" from "got nothing". Confidence: 0.9
- When given a detailed bug report that claims a specific deployment error (e.g., an ImportError, missing symbol, crash log), first reproduce it empirically — read the file, run the import in a fresh container, execute the exact production command — before touching any code. If the error does not reproduce, state plainly that the premise doesn't match reality with the evidence gathered, and explicitly refuse to invent a fix or rename/move symbols just to satisfy a non-existent failure. Honest "the bug isn't here" with verification > inventing plausible-looking code changes to match a fabricated symptom. Confidence: 0.95
- Taste/preference files under `.commandcode/` are local session data and must NEVER be staged, committed, or pushed to project repositories. When committing project changes, explicitly exclude the taste directory from `git add` (stage specific project paths instead of `git add .` or `git add -A`) so the agent's learning data stays local and doesn't pollute the repo. Confidence: 0.9
